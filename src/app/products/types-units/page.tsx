import RenderOptions from "../../components/Table/options";
import { typesUnitsOptions } from "@/utils/listOptions/types-unitsOptions";

export default function HomeProducts() {
  return <RenderOptions options={typesUnitsOptions} />;
}
